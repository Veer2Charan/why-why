var mouseEvent=""

canvas=
document.getElementById("myCanvas");
ctx=canvas.getContext("2d");

var color=document.getElementById("color");
var radius=document.getElementById("radius");
var width=document.getElementById("width");
addEventListener(my mousedown,mousedown)

var mouseEvent="mousedown"
addEventListener(my mouseup, mouseup)
mouseup (e) {
    var mouseEvent="mouseup"
}
addEventListener(my mousedown,mousedown)

var mouseEvent="mouseup"
addEventListener(my mouseleave, mouseleave)
mouseleave (e) {
    var mouseEvent="mouseleave"
}
addEventListener(my mousemove,mousemove)

var mouseEvent="mousemove"
addEventListener(my mousel, mouseup)
mouseup (e) {
    var mouseEvent="mouseup"
}


